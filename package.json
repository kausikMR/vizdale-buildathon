{
  "name": "vizdale-buildathon",
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "dev:web": "npm run dev -w web",
    "dev:api": "npm run dev -w api"
  },
  "private": true,
  "workspaces": [
    "apps/*"
  ]
}
