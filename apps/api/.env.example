PORT=4000

# Postgres connection string. When unset, the API runs an embedded
# PGlite database instead, so it works with no database installed.
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/temple_crm

# Password given to the three seeded demo accounts on first boot.
# Accounts that already have a password are never overwritten.
DEMO_PASSWORD=Temple@123
